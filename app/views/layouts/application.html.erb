<!DOCTYPE html>
<html>
  <head>
    <title>AlexCycle</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main>
      <header class ="flex flex-row justify-between w-full h-1/3 py-6 bg-validera-darker">
        <h1 class="flex flex-col items-start pl-4 font-bold text-6xl text-white">Alex Cycle</h1>
        <div class="flex justify-center items-center font-bold text-3xl"><%= link_to 'Sign out',  destroy_user_session_path, method: :get, class: "font-bold text-3xl text-validera-darker" %></div>
        <div class="flex justify-center items-center font-bold text-3xl"><%= link_to 'Sign in',  user_session_path, method: :post, class: "text-validera-darker" %></div>
      </header>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
      <%= yield %>
    </main>
  </body>
</html>
